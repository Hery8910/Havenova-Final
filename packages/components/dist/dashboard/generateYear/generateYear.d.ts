import { Schedules, WorkDaySettings, Calendar } from '../../../types/calendar';
declare const generateYear: (clientId: string, year: number, schedules: Schedules, blockHolidays: boolean, workDaySettings: WorkDaySettings, available: boolean) => Calendar;
export default generateYear;
//# sourceMappingURL=generateYear.d.ts.map